export enum Routes {
  HOME = "/",
  LOGIN = "/login",
  BOOKS = "/books",
  CHARACTERS = "/characters",
  HOUSES = "/houses",
  HOUSE_DETAILS = "/houses/:id",
  CHARACTER_DETAILS = "/characters/:id",
  BOOK_DETAILS = "/books/:id",
}
