export interface House {
  readonly url: string
  readonly name: string
  readonly region: string
  readonly coatOfArms: string
  readonly words: string
  readonly titles: string[]
  readonly seats: string[]
  readonly currentLord: string
  readonly heir: string
  readonly overlord: string
  readonly founded: string
  readonly founder: string
  readonly diedOut: string
  readonly ancestralWeapons: string[]
  readonly cadetBranches: string[]
  readonly swornMembers: string[]
}
